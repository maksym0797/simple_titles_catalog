import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    paddingContainer: {
        flex: 1,
        backgroundColor: '#fff',
        paddingVertical: 25,
        paddingHorizontal: 10
    },
});
